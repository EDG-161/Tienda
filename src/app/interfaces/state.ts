export interface State {
  id: string;
  codeState: string;
  name: string;
  image: string;
  madeFromGeoData?: boolean;
  colorFill?: string;
  handicraftsRelated?: string;
  description?: string;
}
